# Security Configuration
Section "ServerFlags"
    # Disable Ctrl+Alt+Backspace to prevent accidental X server termination
    Option "DontZap" "true"
    # Allow users to switch VTs (Ctrl+Alt+F1, etc)
    Option "DontVTSwitch" "false"
EndSection